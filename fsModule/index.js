var fs = require('fs');
var http = require('http');

var server = http.createServer(function(req, res) {
    if (req.url === '/') {




    }
}); 

server.listen(4040);
console.log("Server is running!");